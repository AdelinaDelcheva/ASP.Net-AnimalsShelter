@model AnimalBreedInputModel
@{
	ViewData["Title"] = "Add Breed";

}


<div class="container mt-5">
		<h2 class="mb-3">@ViewData["Title"]</h2>

	<form id="categoryForm" enctype="multipart/form-data" method="post">
		<div class="mb-3">
			<label asp-for="Breed" class="form-label">Breed Name</label>
			<input asp-for="Breed" class="form-control" autocomplete="Name" aria-required="true" />
			<span asp-validation-for="Breed" class="text-danger"></span>
		</div>
		
		<div>

			<button type="submit" asp-area="Admin" asp-controller="Breed" asp-action="Add" class="btn btn-primary">Add Breed</button>
		</div>
	</form>
</div>

<!-- Add the Bootstrap JS script -->
@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}